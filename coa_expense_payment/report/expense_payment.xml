<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <report id="expense_payment_report"
            model="expense.payment"
            string="Expense Payment"
            report_type="qweb-pdf"
            name="arabian_expense_payment.expense_payment_template"
            file="arabian_expense_payment.expense_payment_template"
            attachment_use="True"
            attachment="(object.name or '')+' '+ time.strftime('%d,%b %Y %I:%M %p')"
            print_report_name="(object._get_report_base_filename())"
    />
</odoo>

<?xml version="1.0" encoding="UTF-8" ?>
<odoo>
    <data>
        <record id="view_order_line_form_no_decimal" model="ir.ui.view">
            <field name="name">sale.order.line.form.no.decimal</field>
            <field name="model">sale.order.line</field>
            <field name="inherit_id" ref="sale.view_order_form"/>
            <field name="arch" type="xml">
                <xpath expr="//field[@name='order_line']//list//field[@name='price_unit']" position="attributes">
                    <attribute name="options">{'precision': 0}</attribute>
                </xpath>
            </field>
        </record>
        <!-- explicit list view definition -->
        <!--
            <record model="ir.ui.view" id="arabian_digits_numbers.list">
              <field name="name">arabian_digits_numbers list</field>
              <field name="model">arabian_digits_numbers.arabian_digits_numbers</field>
              <field name="arch" type="xml">
                <list>
                  <field name="name"/>
                  <field name="value"/>
                  <field name="value2"/>
                </list>
              </field>
            </record>
        -->

        <!-- actions opening views on models -->
        <!--
            <record model="ir.actions.act_window" id="arabian_digits_numbers.action_window">
              <field name="name">arabian_digits_numbers window</field>
              <field name="res_model">arabian_digits_numbers.arabian_digits_numbers</field>
              <field name="view_mode">list,form</field>
            </record>
        -->

        <!-- server action to the one above -->
        <!--
            <record model="ir.actions.server" id="arabian_digits_numbers.action_server">
              <field name="name">arabian_digits_numbers server</field>
              <field name="model_id" ref="model_arabian_digits_numbers_arabian_digits_numbers"/>
              <field name="state">code</field>
              <field name="code">
                action = {
                  "type": "ir.actions.act_window",
                  "view_mode": "list,form",
                  "res_model": model._name,
                }
              </field>
            </record>
        -->

        <!-- Top menu item -->
        <!--
            <menuitem name="arabian_digits_numbers" id="arabian_digits_numbers.menu_root"/>
        -->
        <!-- menu categories -->
        <!--
            <menuitem name="Menu 1" id="arabian_digits_numbers.menu_1" parent="arabian_digits_numbers.menu_root"/>
            <menuitem name="Menu 2" id="arabian_digits_numbers.menu_2" parent="arabian_digits_numbers.menu_root"/>
        -->
        <!-- actions -->
        <!--
            <menuitem name="List" id="arabian_digits_numbers.menu_1_list" parent="arabian_digits_numbers.menu_1"
                      action="arabian_digits_numbers.action_window"/>
            <menuitem name="Server to list" id="arabian_digits_numbers" parent="arabian_digits_numbers.menu_2"
                      action="arabian_digits_numbers.action_server"/>
        -->
    </data>
</odoo>

<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data noupdate="1">
        <record id="action_migrate_company_data" model="ir.actions.server">
            <field name="name">Migrate Data to Company 2</field>
            <field name="model_id" ref="base.model_res_partner"/>
            <field name="state">code</field>
            <field name="code">
# --- 1. جعل البيانات الأساسية مشتركة (بدون شركة) ---
# العملاء والموردين
partners = env['res.partner'].search([('company_id', '=', 1)]) # افترض أن 1 هو ID الشركة القديمة
partners.write({'company_id': False})

# المنتجات
products = env['product.template'].search([('company_id', '=', 1)])
products.write({'company_id': False})

# --- 2. نسخ حركات محددة (أوامر البيع كمثال) للشركة الجديدة ---
new_company_id = 2 # افترض أن 2 هو ID الشركة الجديدة

# جلب أوامر البيع للشركة القديمة (يفضل نقل الـ Draft فقط لتجنب المشاكل)
sales = env['sale.order'].search([
    ('company_id', '=', 1), 
    ('state', 'in', ['draft', 'sent'])
])

for sale in sales:
    # استخدام copy() بيضمن إنشاء السجل الجديد بشكل سليم وتوليد Sequence جديد
    sale.copy(default={'company_id': new_company_id})

# يمكنك تكرار نفس الـ loop مع (purchase.order) وغيرها من الحركات البسيطة
            </field>
        </record>
    </data>
</odoo>
<?xml version="1.0" encoding="UTF-8" ?>

<odoo>
    <record id="sale_order_form" model="ir.ui.view">
        <field name="name">sale.order.form</field>
        <field name="model">sale.order</field>
        <field name="inherit_id" ref="sale.view_order_form"/>
        <field name="arch" type="xml">
            <xpath expr="//header" position="inside">
                <button name="action_create_con_project" string="Create Construction Project" type="object"
                        invisible="state != 'sale'" class="btn btn-primary"/>
            </xpath>
            <xpath expr="//div[contains(@class, 'oe_button_box')]" position="inside">
                <button name="action_view_construction_projects"
                        type="object"
                        string="Construction Projects"
                        invisible="construction_count == 0"
                        class="oe_stat_button"
                        icon="fa-building">
                    <field name="construction_count" widget="statinfo"/>
                </button>
            </xpath>
            <xpath expr="//div[contains(@class, 'oe_title')]" position="replace">
                <div class="oe_title">
                    <h1>
                        <field name="name"/>
                    </h1>
                </div>

            </xpath>
             <xpath expr="//field[@name='partner_id']" position="after">
                <field name="project_name" invisible="state != 'sale'"/>
            </xpath>
        </field>
    </record>
</odoo>
<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <!--
        قاعدة للمستخدم العادي (غير المدير):
        - لو Own Documents Only = True  → يشوف سجلاته هو بس
        - لو Own Documents Only = False → يشوف الكل
        المدير يشوف الكل دائماً بغض النظر عن الإعداد
    -->

    <!-- قاعدة المستخدم العادي: conditional على إعداد الشركة -->
    <record id="rule_commission_line_own" model="ir.rule">
        <field name="name">حساب العمولة: Own Documents Only</field>
        <field name="model_id" ref="model_commission_line"/>
        <field name="domain_force">
            [('salesperson_id', '=', user.id)] if user.company_id.commission_own_documents else [(1, '=', 1)]
        </field>
        <field name="groups" eval="[(4, ref('base.group_user'))]"/>
        <field name="perm_read" eval="True"/>
        <field name="perm_write" eval="True"/>
        <field name="perm_create" eval="True"/>
        <field name="perm_unlink" eval="True"/>
    </record>

    <!-- قاعدة المدير: يشوف الكل دائماً -->
    <record id="rule_commission_line_manager" model="ir.rule">
        <field name="name">حساب العمولة: المدير يرى الكل</field>
        <field name="model_id" ref="model_commission_line"/>
        <field name="domain_force">[(1, '=', 1)]</field>
        <field name="groups" eval="[(4, ref('sales_team.group_sale_manager'))]"/>
        <field name="perm_read" eval="True"/>
        <field name="perm_write" eval="True"/>
        <field name="perm_create" eval="True"/>
        <field name="perm_unlink" eval="True"/>
    </record>

</odoo>

<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data>

        <!--
            أضف context للـ product_id في سطور الأوردر
            عشان يظهر الكمية في الـ dropdown
        -->
        <record id="sale_order_form_stock_info" model="ir.ui.view">
            <field name="name">sale.order.form.stock.info</field>
            <field name="model">sale.order</field>
            <field name="inherit_id" ref="sale.view_order_form"/>
            <field name="arch" type="xml">

                <xpath expr="//field[@name='order_line']/list//field[@name='product_id']" position="attributes">
                    <attribute name="context">{'partner_id': parent.partner_id, 'quantity': product_uom_qty, 'pricelist': parent.pricelist_id, 'uom': product_uom, 'company_id': parent.company_id, 'default_lst_price': price_unit, 'default_uom_id': product_uom, 'show_sale_stock': True}</attribute>
                    <attribute name="options">{"no_open": true, "no_quick_create": true}</attribute>
                </xpath>

            </field>
        </record>

    </data>
</odoo>

<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <!-- Inherit Location Form View: add code after name -->
    <record id="view_location_form_code" model="ir.ui.view">
        <field name="name">stock.location.form.code</field>
        <field name="model">stock.location</field>
        <field name="inherit_id" ref="stock.view_location_form"/>
        <field name="arch" type="xml">
            <field name="name" position="after">
                <field name="location_code" placeholder="e.g. LOC-001"/>
            </field>
        </field>
    </record>

    <!-- Inherit Location Tree View: add code column after name -->
    <record id="view_location_tree_code" model="ir.ui.view">
        <field name="name">stock.location.tree.code</field>
        <field name="model">stock.location</field>
        <field name="inherit_id" ref="stock.view_location_tree2"/>
        <field name="arch" type="xml">
            <field name="complete_name" position="after">
                <field name="location_code"/>
            </field>
        </field>
    </record>

</odoo>
